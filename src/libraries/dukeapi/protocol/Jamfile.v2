#
# dukeapi
#
using pb : $(PROTOC) ;

project 
	: requirements 
		<library>/user-config//protobuf
    : usage-requirements 
		<library>/user-config//protobuf
	;

lib allpb : 
			[ glob-tree *.proto ]
		;
explicit allpb ;
