import os ;

local BOOST_ROOT		= [ modules.peek : BOOST_ROOT ] ;

use-project /boost : $(BOOST_ROOT) ;

project 
	: requirements
		<include>.
		<library>/boost//thread
	: usage-requirements
		<include>.
		<library>/boost//thread
	; 

lib PreemptiveLib
			:
				[ glob *.cpp ]
			;
					
explicit PreemptiveLib ;