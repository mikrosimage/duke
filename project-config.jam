import path ;

project project-config ;

if ! $(duke_site_configuration) {
	EXIT "
To build 'duke' you need to setup a few libraries.
Please customize 'site-config.jam.example' and move it to (BOOST_ROOT)/tools/build/v2/site-config.jam

NB : When done, don't forget to uncomment the file's last line" ;
}
